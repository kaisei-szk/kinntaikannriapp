# アルバイト勤怠管理アプリ 要件定義書

## 1. 概要

店舗に固定設置した Android タブレット 1 台で完結する、アルバイト向け勤怠打刻アプリ。
スタッフは名前ボタンをタップするだけで出退勤を記録でき、店長は管理画面から記録の閲覧・CSV 出力ができる。

| 項目 | 内容 |
| --- | --- |
| 利用端末 | NEC 製 Android タブレット × 1台（店舗固定・キオスク運用） |
| 稼働方式 | **タブレット単体のローカル運用**（Termux 上で Node.js サーバーを起動し、同一端末のブラウザから `localhost` でアクセス） |
| ネットワーク | 不要（外部サーバー・Tailscale 等は使用しない） |
| 公開方法 | ストア非公開。Web アプリとして自作し、Chrome の「ホーム画面に追加」で運用 |
| 想定人数 | 30 名以上（将来の人数増にも耐える設計） |
| コスト | 完全無料（OSS・端末内完結のためサーバー費・通信費ゼロ） |
| 開発環境 | Mac 上で開発・動作確認 → リポジトリをタブレットへ転送してデプロイ |

---

## 2. 前提・方針

- サーバーとクライアントは同一タブレット内で完結する。Termux で Node.js + Express を起動し、Chrome から `http://localhost:3000`（ポートは仮）にアクセスする。
- `localhost` アクセスのため HTTPS 化は不要（カメラ API も `localhost` はセキュアコンテキスト扱いで動作する）。
- **顔写真は「撮影の演出」のみ**。カメラを起動しプレビュー＋シャッター動作を見せることで不正打刻の抑止とするが、**画像データは保存しない**。
- タブレットは常に打刻画面のみを表示するキオスク運用とする（Android の画面固定機能を使用）。

---

## 3. 機能要件

### 3.1 スタッフ向け画面（打刻画面）

| # | 機能 | 詳細 | 優先度 |
| --- | --- | --- | --- |
| S-01 | 名前一覧表示 | 在籍スタッフの名前をボタン形式で一覧表示。ログイン不要。30名以上でも探しやすいレイアウト（五十音順・グリッド表示・スクロール対応） | 必須 |
| S-02 | 出勤/退勤の自動判定 | 名前タップ時、その人の当日の最終打刻状態から「出勤」「退勤」を自動判定して提示（手動で切り替えも可能） | 必須 |
| S-03 | 確認画面 | 「山田さん、出勤で合っていますか？」の確認ダイアログを一段挟み、誤タップを防止 | 必須 |
| S-04 | カメラ演出 | 確認後にインカメラを自動起動し、プレビュー表示＋シャッター動作を行う。**画像は保存しない**（本人確認の抑止効果のみ） | 必須 |
| S-05 | 打刻記録 | 「日時・スタッフ名・出勤/退勤」をサーバー（同一端末内）に記録 | 必須 |
| S-06 | 完了表示・自動復帰 | 「打刻完了」画面を数秒表示し、自動でトップ（名前一覧）に戻る | 必須 |
| S-07 | 誤タップ防止 UI | 名前ボタンは十分な大きさ・間隔を確保。連続タップ・二重送信を防止 | 必須 |

### 3.2 管理者向け画面（店長用）

同一アプリ内の別ページとして用意し、パスワードで保護する。

| # | 機能 | 詳細 | 優先度 |
| --- | --- | --- | --- |
| A-01 | 管理者認証 | パスワードによるアクセス制限（打刻画面からは導線を目立たせない） | 必須 |
| A-02 | スタッフ管理 | スタッフの登録・編集・削除（削除は論理削除とし、過去の打刻記録は保持） | 必須 |
| A-03 | 勤怠記録一覧 | 日別・月別で「誰が何時に出退勤したか」を一覧表示。スタッフ別の絞り込み | 必須 |
| A-04 | CSV エクスポート | 給与計算用に月別の勤怠記録を CSV 出力（ダウンロード or 端末内保存） | 必須 |
| A-05 | 打刻の手動修正 | 打刻漏れ・ミスの補正（追加・時刻修正・削除）。修正履歴が分かるようにする | 推奨 |

---

## 4. データ設計

- データベース: **SQLite**（ファイル 1 つで完結、追加インストール不要）

| テーブル | カラム |
| --- | --- |
| スタッフ | ID・名前・表示順・在籍フラグ（論理削除用） |
| 打刻 | ID・スタッフID・種別（出勤/退勤）・日時・手動修正フラグ |

> 写真は保存しないため、写真ファイルパス列・画像保存フォルダは**設けない**。

---

## 5. システム構成

| 項目 | 内容 |
| --- | --- |
| 実行環境 | Node.js + Express（フロント画面も API も同一プロセスで配信） |
| 稼働場所 | タブレット内の Termux |
| 自動起動 | Termux:Boot で端末再起動時にサーバーを自動起動。プロセス異常終了時は再起動スクリプトで復帰 |
| アクセス | 同一端末の Chrome から `localhost` へアクセス（PWA として「ホーム画面に追加」） |

※ 単一プロセス・単一端末構成のため pm2 は必須ではない。Termux 上でのシンプルな起動スクリプト＋Termux:Boot で代替する。

---

## 6. 開発・デプロイフロー

1. **開発は Mac 上で行う**（Node.js をローカル起動し、ブラウザで動作確認。カメラ演出も Mac のカメラで確認可能）
2. Git リポジトリで管理し、完成したらタブレットへ転送する
   - 転送方法: Termux 上での `git clone` / `git pull`（推奨）、または zip を端末へコピーして展開
3. タブレットの Termux で `npm install` → サーバー起動 → Chrome で動作確認
4. 以後の更新も「Mac で開発 → リポジトリ経由でタブレットに反映」を基本とする

---

## 7. タブレット側の設定（NEC 製 Android タブレット）

- Termux・Termux:Boot をインストール（F-Droid 経由。Play ストア版は非推奨）
- Termux をバッテリー最適化の対象外に設定（バックグラウンドでサーバーが落ちないように）
- Chrome でアプリ URL を開き「ホーム画面に追加」してアイコン化
- Android 設定で「画面固定（アプリ固定）」を ON にし、打刻画面のみ表示するキオスク運用にする
- 画面の自動スリープ設定を運用に合わせて調整（常時表示 or 短時間スリープ）

※ NEC タブレットは機種によって Android バージョン・設定メニュー名が異なるため、実機で「画面固定」「バッテリー最適化除外」の手順を確認する。

---

## 8. バックアップ

- 対象: **SQLite ファイルのみ**（写真は保存しないためデータ量は小さい）
- 週 1 回程度、USB メモリや Google ドライブ等へコピー（手動 or 簡単なスクリプトで自動化）
- タブレット単体運用のため必須運用とする（端末故障＝全記録消失のリスクがある）
- 管理画面の CSV エクスポートも簡易バックアップとして併用できる

---

## 9. 非機能要件

| 項目 | 要件 |
| --- | --- |
| 性能 | 打刻操作はタップから完了表示まで数秒以内。30名以上・数年分の記録でも一覧表示が実用速度で動くこと |
| 可用性 | 端末再起動後もサーバーが自動復帰すること（Termux:Boot） |
| セキュリティ | 管理画面はパスワード保護。サーバーは `localhost` のみで待ち受け、外部からアクセス不可 |
| 保守性 | Mac からリポジトリ経由で更新を反映できること。DB はファイルコピーだけでバックアップ・復元できること |
| 拡張性 | スタッフ増（30名→それ以上）に UI・DB とも耐えられること |

---

## 10. スコープ外（今回はやらないこと）

- 顔写真の保存・管理画面での写真確認（カメラは演出のみ）
- 外部サーバー・常時稼働 PC での運用、Tailscale 等によるネットワーク接続
- 複数店舗・複数端末での同期
- ストアへの公開、給与計算そのもの（CSV 出力まで）

---

## 11. 開発の進め方（推奨順）

1. Mac 上で最小機能を作る：「名前一覧表示 → タップ → 確認 → カメラ演出 → 打刻記録（SQLite 保存）→ 完了表示」
2. 管理画面を追加する（認証・スタッフ管理・記録一覧・CSV 出力）
3. 打刻の手動修正など補助機能を追加する
4. タブレットに Termux 環境を作り、リポジトリを転送して実機で動作確認する
5. キオスクモード・自動起動（Termux:Boot）・バックアップ運用を整えて本稼働
