# 管理画面パスワード(いずれか一方を設定)
# 推奨: `npm run set-admin-password` を実行して表示されたハッシュを設定する
ADMIN_PASSWORD_HASH=

# 簡易設定(開発用途向け・平文)。ADMIN_PASSWORD_HASH が設定されている場合はそちらが優先される
ADMIN_PASSWORD=

# セッション用の秘密鍵。未設定の場合は初回起動時に自動生成され data/.session-secret に保存される
SESSION_SECRET=

# サーバーの待受ポート
PORT=3000
